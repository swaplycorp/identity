/// Oauth implements an oauth authorization server (i.e., a set of endpoints that allow third party
/// applications to consume OIDC and Oauth 2.0 endpoints for user data), and an Oauth-consuming
/// login provider (i.e., a set of endpoints that allow a user to login to swaply via some third
/// party).
pub mod oauth;
